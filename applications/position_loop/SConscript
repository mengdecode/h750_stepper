from building import *

cwd = GetCurrentDir()
src = []
path = [cwd, cwd + '/../stepper_drv']

if GetDepend('BSP_USING_STEPPER_POS_LOOP'):
    src += Glob('*.c')

group = DefineGroup('position_loop', src, depend=['BSP_USING_STEPPER_POS_LOOP'], CPPPATH=path)
Return('group')
